const mysql = require('mysql2/promise'); require('dotenv').config({ path: __dirname + '/.env' }); async function run() { const connection = await mysql.createConnection({ host: process.env.DB_HOST, user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME, port: process.env.DB_PORT }); const now = new Date(); await connection.execute('UPDATE container_inspections SET date = ? WHERE id = 1', [now]); const [rows] = await connection.query('SELECT date FROM container_inspections WHERE id = 1'); console.log('Original:', now); console.log('From DB:', rows[0].date); process.exit(0); } run();
