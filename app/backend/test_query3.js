const mysql = require('mysql2/promise'); require('dotenv').config(); async function run() { const c = await mysql.createConnection({ host: process.env.DB_HOST, user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME, port: process.env.DB_PORT }); const [r] = await c.query('SELECT container, LENGTH(photo1), LEFT(photo1, 100) as preview FROM container_inspections WHERE container LIKE \'%%TGHU2345678%%\''); console.log(r); process.exit(0); } run();  
